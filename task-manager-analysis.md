Exercise Part 1: Understanding Project StructureInitial Understanding (Before AI)
Coming from a C++ background, the codebase reminds me of how .cpp and .h files are organized; it looks highly structured. Instead of everything sitting in one root folder, the code is split into a standard Java layout (src/main/java for the actual code and src/test/java for the testing code). The primary language is pure Java. It uses Gradle as its build system and dependency manager (similar to CMake or Makefiles) , and almost certainly uses a framework like JUnit for testing. The code is divided into specific packages: app, cli, model, storage, and util.Documented Findings (After AI Analysis)Misconceptions: Initially, I assumed this was a raw Java application. The AI corrected this by analyzing the Gradle file, revealing a robust tech stack relying on external libraries: Google Gson for saving to JSON, Apache Commons CLI for parsing terminal commands, and Mockito/JUnit 5 for testing. Furthermore, I assumed the util folder just held generic helper functions. The AI revealed it actually contains the heaviest business rules (like regex parsing and remote syncing), keeping the core Task model clean and lightweight.Entry Points & Architectural Patterns: The main execution starts in TaskManagerCli.java, as Gradle is explicitly configured to look here when running ./gradlew run. The project strictly follows a Separation of Concerns using a Controller/Facade pattern, routing everything through the Business Logic layer.Key Components & Responsibilities: * model: The pure data layer containing domain entities.storage: The persistence layer responsible for file I/O via Gson.util: The stateless service layer containing complex factories/calculators.app: The controller layer enforcing rules and coordinating inputs.cli: The presentation layer handling the terminal interface.Exercise Part 2: Finding Feature Implementation ("Task Export to CSV")Initial Search & Hypothesis
Using search terms like "file", "write", "save", and "Gson", I found that all current file I/O operations are localized inside storage/TaskStorage.java. I hypothesized that the CSV file writing logic should also live in the storage package. I assumed I would need to modify the CLI for a new --export-csv flag, update TaskManager.java to intercept the command, and add standard FileWriter logic into the storage layer.Documented Findings (After AI Analysis)Implementation Map: The AI clarified that the feature does not currently exist and requires touching three distinct layers. The core CSV formatting logic should actually live in a brand new file within the util/ package, not storage/, to maintain Separation of Concerns.Affected Components: cli/TaskManagerCli.java (to capture the command) , app/TaskManager.java (orchestrator method) , and a new util/TaskCsvExporter.java.Implementation Plan:Create TaskCsvExporter.java in the util package with a method taking a List<Task> and a file path.Write data transformation logic to flatten List<String> tags with pipes (e.g., tag1|tag2) and convert LocalDateTime to avoid breaking the CSV comma format.Use standard Java I/O (FileWriter) to write the headers and transformed strings, catching any IOException.Update the TaskManager to call this utility, and update the CLI to trigger the manager.Exercise Part 3: Understanding Domain ModelExtract Domain Model & Initial Understanding
The core entities are model/Task.java, model/TaskStatus.java, and model/TaskPriority.java. The Task class is the main blueprint. Instead of using raw strings for a task's state or urgency, the Task object likely contains instances of these other two Enums to ensure data consistency.Test Knowledge & Documented FindingsDomain Validation: The AI confirmed the system models the lifecycle of a to-do item, with Enums strictly controlling state to prevent invalid data entry.Revised Diagram: The relationship is a simple composition: Task <---has a--- TaskStatus, and Task <---has a--- TaskPriority.Knowledge Testing: The AI quizzed me on edge cases, revealing that the current isOverdue() logic might unfairly penalize tasks actively in REVIEW status. It also highlighted that modifying domain models requires updating metadata (like the updatedAt timestamp) to maintain data integrity, proving why encapsulation is critical to protect the object's state.Domain GlossaryTask: The central blueprint entity representing a single to-do item and its metadata.TaskStatus: An Enum strictly defining the current lifecycle state of a task (e.g., IN_PROGRESS, REVIEW).TaskPriority: An Enum strictly defining the urgency level of a task.Exercise Part 4: Practical Application & ReflectionScenario Planning: 7-Day Overdue RuleTo automatically mark tasks overdue by 7 days as abandoned (unless high priority), I will modify the following:model/TaskStatus.java: Add a new ABANDONED enum state.model/Task.java: Add domain logic (shouldBeAbandoned() and markAsAbandoned()) so the task evaluates its own dates without exposing internal fields.app/TaskManager.java: Add the orchestration loop (autoAbandonOverdueTasks()) to fetch tasks, check the domain logic, and save updates to storage.cli/TaskManagerCli.java: Add a trigger for the cleanup check.Questions for the Team Lead before implementing:Should this cleanup script run on a background cron job, every time the app launches, or only when a user manually types a command? Does the rule exempt URGENT tasks as well as HIGH priority? Should we exempt tasks actively IN_PROGRESS or in REVIEW, even if they are 7 days overdue? Final Reflection
The AI was crucial for understanding Separation of Concerns. It stopped me from writing messy, monolithic code by separating data transformation into a util file and splitting the business rules between the model's state and the manager's orchestration. It also surfaced edge-case questions I wouldn't have considered.While I am still getting used to how Gradle connects files at compile-time without explicitly importing every single dependency manually , my next steps are to write the code for these features and run the existing JUnit test suite (./gradlew test) to ensure I haven't broken the existing architecture